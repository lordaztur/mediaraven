# Reference

Documentation of internal helpers and the logging layer — useful to understand the code if you're going to extend the bot.

## Pages

- **[Internal helpers](helpers.md)** — `cfg()`, `lmsg()`, `msg()`, ContextVar, validators
- **[Logging](logging.md)** — log format, levels, silenced libs, stack trace capture
